# Production dependencies
# Install with: pip install -r requirements/production.txt

# ============================================================================
# Core Dependencies (from base.txt)
# ============================================================================

# Core Django framework
Django==5.1.4

# PostgreSQL database adapter
psycopg2-binary==2.9.9

# Django REST Framework for API development
djangorestframework==3.14.0

# JWT authentication for API (Feature 013)
djangorestframework-simplejwt==5.3.1

# OpenAPI documentation generation (Feature 013)
drf-spectacular==0.27.0

# CORS headers for frontend development
django-cors-headers==4.3.1

# Django Redis for caching and rate limiting
django-redis==5.4.0

# Prometheus metrics and monitoring
django-prometheus==2.3.1
prometheus-client==0.19.0

# Environment variable management
django-environ==0.11.2

# Security Baseline Runtime Dependencies (Feature 003)
# YAML parsing for security manifests
pyyaml>=6.0.1

# Bloom filter for password breach detection (SEC020)
pybloom-live>=3.1.0

# Filtering support
django-filter==24.2

# Async Task Execution (B15)
celery[redis]>=5.3.0,<6.0
redis>=5.0.0,<6.0

# HTTP client for webhook notifications
requests>=2.31.0,<3.0

# AI Generation Pipeline (B34)
openai>=1.0.0
langgraph-sdk>=0.1.0
google-genai>=1.0.0  # Google Gemini / Imagen (asset generation)
minimax-python>=0.2.0  # MiniMax / Hailuo video generation

# AWS S3 Storage (B35 File Storage)
boto3>=1.34.0,<2.0

# Scaffolding CLI (B20)
click>=8.1.0,<9.0
jinja2>=3.1.0,<4.0  # Template rendering engine

# WebSocket Infrastructure (B23)
channels[daphne]>=4.0.0,<5.0
channels-redis>=4.1.0,<5.0
PyJWT>=2.8.0,<3.0

# Image processing (WP03)
Pillow>=10.0.0,<11.0

# Background removal for asset processing (rembg uses U2-Net ONNX model)
# Force rebuild: 2026-02-13
rembg>=2.0.50,<3.0

# Explicit runtime dependency for rembg (avoids: No module named 'onnxruntime')
onnxruntime>=1.17.0,<2.0

# Numpy 2.x currently triggers runtime compatibility issues for some compiled
# dependencies we use in asset/video processing (and can break torch numpy
# bridging). Keep production on the stable 1.x line for now.
numpy<2

# Static FFmpeg binary with full codec support (VP9 alpha, etc.).
# The system FFmpeg from Debian apt does NOT support VP9 alpha encoding.
imageio-ffmpeg>=0.5.1

# RVM (Robust Video Matting) requires PyTorch.
# IMPORTANT: we want CPU-only wheels in production to avoid pulling CUDA/NVIDIA runtime
# wheels (which are huge and unnecessary on Railway CPU machines).
#
# The "+cpu" build tags are hosted on the PyTorch CPU index.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu

# ============================================================================
# Production-Only Dependencies
# ============================================================================

# WSGI HTTP Server
gunicorn==21.2.0

# Static files serving with compression
whitenoise==6.6.0

# Database URL parsing for Render/Railway
dj-database-url==2.1.0

# Structured logging for production
python-json-logger==2.0.7
uvicorn==0.27.1

# Structured logging for media pipeline operations
structlog>=24.0.0
