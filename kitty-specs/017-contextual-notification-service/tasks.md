# Work Packages: Contextual Notification Service (B17)
*Path: [kitty-specs/017-contextual-notification-service/tasks.md](kitty-specs/017-contextual-notification-service/tasks.md)*

**Feature**: B17 Contextual Notification Service
**Branch**: `017-contextual-notification-service`
**Inputs**: spec.md, plan.md, research.md, data-model.md, contracts/, quickstart.md

**Constitutional Compliance**: All tasks align with Django Core-App Constitution principles (`.kittify/memory/constitution.md`).

**Organization**: Fine-grained subtasks (`Txxx`) roll up into work packages (`WPxx`). Each work package is independently deliverable and testable.

**Prompt Files**: Each work package references a matching prompt file in `kitty-specs/017-contextual-notification-service/tasks/planned/` generated by `/spec-kitty.tasks`.

## Subtask Format: `[Txxx] [P?] Description`
- **[P]** indicates the subtask can proceed in parallel (different files/components).
- File paths are absolute or relative to repo root.

---

## Phase 0: Setup & Foundation (Priority: P0)

### Work Package WP01: Django App Scaffolding & Configuration

**Goal**: Create `contextual_notifications` Django app with models, migrations, and admin configuration.
**Independent Test**: App loads without errors, models are registered in admin, migrations apply cleanly.
**Prompt**: `tasks/planned/WP01-django-app-scaffolding.md`
**Priority**: P0 (Foundation)

#### Included Subtasks
- [ ] T001 Create `src/contextual_notifications/` Django app structure
- [ ] T002 [P] Create `RoutingRule` model with indexes in `models/routing_rule.py`
- [ ] T003 [P] Create `NotificationPreference` model with indexes in `models/notification_preference.py`
- [ ] T004 [P] Create `OrganisationNotificationPolicy` model in `models/org_notification_policy.py`
- [ ] T005 Create model managers for query optimization in `models/managers.py`
- [ ] T006 Configure Django admin for all three models in `admin.py`
- [ ] T007 Generate and apply initial migration `0001_initial.py`
- [ ] T008 Add app to `INSTALLED_APPS` in config/settings

#### Constitutional Alignment
- Principle II (Architecture): Single-responsibility app, clear model separation
- Principle III (Code Quality): Type hints on all model fields
- Principle VI (Performance): Explicit indexes on hot query paths

#### Implementation Notes
- Use explicit column fields (not JSON) for routing rules per research.md decision
- Add composite indexes: `(event_type, organisation_id)`, `(event_type, scope)`
- Ensure model `__str__` methods are admin-friendly

#### Parallel Opportunities
- All three models can be created in parallel (T002-T004)

#### Dependencies
- None (starting package)

#### Risks & Mitigations
- Migration conflicts → Apply migrations immediately after creation
- Missing indexes → Review data-model.md index specifications carefully

---

### Work Package WP02: Event Service & Validation

**Goal**: Implement event emission API with validation, making it easy for domain code to emit events.
**Independent Test**: EventService.emit_event() validates event schema, rejects invalid events, queues valid events to Celery.
**Prompt**: `tasks/planned/WP02-event-service-validation.md`
**Priority**: P0 (Foundation)

#### Included Subtasks
- [ ] T009 Create `services/event_service.py` with `emit_event()` method
- [ ] T010 Implement event schema validation (type, context, payload structure)
- [ ] T011 Create custom exceptions in `exceptions.py` (ValidationError, EventServiceError)
- [ ] T012 Add type hints to all service methods
- [ ] T013 Create Celery task `route_event_task` in `tasks/routing_tasks.py`
- [ ] T014 Integrate Celery task call from `emit_event()` (async handoff)
- [ ] T015 [P] Add structured logging for event emission (valid/invalid)
- [ ] T016 [P] Add Prometheus metrics for events emitted, validation failures

#### Constitutional Alignment
- Principle III (Code Quality): Type hints, clear validation logic
- Principle V (Security): Validate at boundary, reject malformed events
- Principle VI (Performance): Async processing via Celery, structured logging, metrics

#### Implementation Notes
- Event schema: `{"type": str, "context": dict, "payload": dict}` per contracts/event-emission-api.md
- Validation rules: event_type matches `^[a-z0-9._]+$`, context.org_id required, payload.title/body required
- Fire-and-forget pattern: emit_event() returns None, exceptions logged but not raised to domain code

#### Parallel Opportunities
- Logging and metrics (T015-T016) can be added in parallel once core service exists

#### Dependencies
- None (no model dependencies, Redis configured in B10)

#### Risks & Mitigations
- Blocking domain operations → Ensure emit_event() completes in <5ms (validation only, no DB calls)
- Celery configuration → Verify Celery broker/workers operational before testing

---

## Phase 1: Core Routing (Priority: P1)

### Work Package WP03: Routing Service & Rule Evaluation

**Goal**: Implement core routing logic that evaluates routing rules to determine target users and channels.
**Independent Test**: Given event + routing rules, RoutingService returns correct list of (user_id, channel) tuples.
**Prompt**: `tasks/planned/WP03-routing-service-rule-evaluation.md`
**Priority**: P1 (Core functionality for User Story 1)

#### Included Subtasks
- [ ] T017 Create `services/routing_service.py` with `route_event()` method
- [ ] T018 Implement rule query logic (filter by event_type, scope, org, project)
- [ ] T019 Implement rule evaluation (match scope global/org/project, AND conditions only)
- [ ] T020 Implement target user resolution (query B08 for users in target_role)
- [ ] T021 Optimize queries with `select_related()` for foreign keys (org, project)
- [ ] T022 Implement priority-based rule ordering (highest priority first)
- [ ] T023 Add type hints to all routing service methods
- [ ] T024 [P] Add structured logging for rule matching (matched rules, target users)
- [ ] T025 [P] Add metrics for routing decisions (time, rules evaluated, users targeted)

#### Constitutional Alignment
- Principle II (Architecture): Service layer encapsulates routing logic, no business logic in models
- Principle VI (Performance): Query optimization, no N+1 queries, select_related usage
- Principle VI (Reliability): Structured logging for debugging, metrics for observability

#### Implementation Notes
- Rule evaluation order: project rules override org rules override global rules
- AND-only conditions: all rule criteria must match (event_type + scope + org/project + target_role)
- Use Django ORM filters, no raw SQL
- Cache global rules in-memory per event batch (optional optimization)

#### Parallel Opportunities
- Logging and metrics (T024-T025) can be added in parallel with core logic

#### Dependencies
- Depends on WP01 (RoutingRule model exists)
- Depends on B08 for user role queries

#### Risks & Mitigations
- Slow queries → Profile queries, ensure indexes are used (EXPLAIN ANALYZE)
- Complex logic → Keep evaluation simple (AND-only), defer OR logic to future

---

### Work Package WP04: User Preferences & Filtering

**Goal**: Implement user preference checking to filter out notifications users have opted out of.
**Independent Test**: Given target users + preferences, filtering service returns users who haven't opted out.
**Prompt**: `tasks/planned/WP04-user-preferences-filtering.md`
**Priority**: P1 (Core functionality for User Story 2)

#### Included Subtasks
- [ ] T026 Create `services/preference_service.py` with `check_preferences()` method
- [ ] T027 Implement bulk preference lookup for target users (avoid N+1)
- [ ] T028 Implement opt-out filtering (remove users with enabled=False for event_type+channel)
- [ ] T029 Handle missing preferences (default to enabled=True)
- [ ] T030 Add type hints to all preference service methods
- [ ] T031 [P] Add structured logging for preference filtering (filtered users count)
- [ ] T032 [P] Add metrics for preference filtering (opt-outs applied)

#### Constitutional Alignment
- Principle VI (Performance): Bulk lookups, no N+1 queries
- Principle VI (Reliability): Structured logging, metrics

#### Implementation Notes
- Bulk query: `NotificationPreference.objects.filter(user__in=target_users, event_type=event_type, channel=channel)`
- Default behavior: if no preference exists, user receives notification (opt-in default)
- Per (user, event_type, channel) granularity per research.md decision

#### Parallel Opportunities
- Logging and metrics (T031-T032) parallel with core logic

#### Dependencies
- Depends on WP01 (NotificationPreference model exists)
- Depends on WP03 (routing service provides target users)

#### Risks & Mitigations
- Performance with large user sets → Batch preference lookups, use `values_list()` for IDs only

---

### Work Package WP05: Suppression Service & Redis Integration

**Goal**: Implement duplicate notification suppression using Redis cache with TTL.
**Independent Test**: Given repeated events for same (user, event_type, resource_id), only first notification passes suppression check.
**Prompt**: `tasks/planned/WP05-suppression-service-redis.md`
**Priority**: P1 (Core functionality for User Story 4)

#### Included Subtasks
- [ ] T033 Create `services/suppression_service.py` with `check_suppression()` and `record_suppression()` methods
- [ ] T034 Implement Redis key generation: `suppression:{user_id}:{event_type}:{resource_id}`
- [ ] T035 Implement atomic suppression check using `cache.add()` (SETNX pattern)
- [ ] T036 Configure TTL (default 300 seconds, configurable per event type)
- [ ] T037 Handle Redis unavailability gracefully (skip suppression, log warning)
- [ ] T038 Add type hints to all suppression service methods
- [ ] T039 [P] Add structured logging for suppression (hits/misses)
- [ ] T040 [P] Add metrics for suppression (cache hits, misses, errors)

#### Constitutional Alignment
- Principle VI (Performance): Fast Redis lookups (<10ms), TTL auto-cleanup
- Principle VI (Reliability): Graceful degradation if Redis unavailable

#### Implementation Notes
- Use django-redis cache backend (already configured by B10)
- Atomic check: `cache.add(key, timestamp, timeout=TTL)` returns False if exists
- Suppression scope: per (user_id, event_type, resource_id) per clarifications
- If Redis fails, log warning and proceed without suppression (better UX than failure)

#### Parallel Opportunities
- Logging and metrics (T039-T040) parallel with core logic

#### Dependencies
- Depends on B10 (django-redis configured)
- Depends on WP03, WP04 (routing + preference services provide filtered users)

#### Risks & Mitigations
- Redis unavailable → Graceful degradation, monitor Redis health
- Key collisions → Use consistent key format, include all three identifiers

---

### Work Package WP06: B16 Integration & Notification Handoff

**Goal**: Integrate with B16 notification service to deliver routed notifications.
**Independent Test**: Given routed event, B16 receives correct notification creation calls (mocked in tests).
**Prompt**: `tasks/planned/WP06-b16-integration-handoff.md`
**Priority**: P1 (Core functionality for User Story 1)

#### Included Subtasks
- [ ] T041 Create `services/notification_handoff_service.py` with `dispatch_to_b16()` method
- [ ] T042 Integrate B16 NotificationService API (direct service call)
- [ ] T043 Map event payload to B16 notification format (title, body, url, priority)
- [ ] T044 Implement per-user notification creation loop
- [ ] T045 Handle B16 errors gracefully (log, don't block routing for other users)
- [ ] T046 Add type hints to all handoff service methods
- [ ] T047 [P] Add structured logging for B16 calls (users notified, channels)
- [ ] T048 [P] Add metrics for B16 handoff (notifications created, B16 errors)

#### Constitutional Alignment
- Principle II (Architecture): Clear separation - B17 routes, B16 delivers
- Principle VI (Reliability): Error handling, don't fail entire batch on single error

#### Implementation Notes
- Direct synchronous call to B16 service layer per research.md decision
- B16 handles async delivery via Celery (B17 doesn't re-queue)
- Map event priority to B16 priority (0=low, 1=normal, 2=high, 3=urgent)
- Loop per user: create notification for each (user_id, channel) pair

#### Parallel Opportunities
- Logging and metrics (T047-T048) parallel with core logic

#### Dependencies
- Depends on B16 NotificationService API
- Depends on WP03-WP05 (routing, preference, suppression services)

#### Risks & Mitigations
- B16 API changes → Integration tests detect breakage, version B16 interface
- Single user failure blocks others → Handle per-user errors independently

---

### Work Package WP07: Audit Logging & Routing Decision Logs

**Goal**: Log all routing decisions to B09 audit system for debugging and compliance.
**Independent Test**: After routing event, audit log contains complete routing decision (rules matched, users targeted, channels selected, suppressed users).
**Prompt**: `tasks/planned/WP07-audit-logging-routing-decisions.md`
**Priority**: P1 (Core functionality for User Story 5)

#### Included Subtasks
- [ ] T049 Create `services/audit_service.py` with `log_routing_decision()` method
- [ ] T050 Integrate B09 AuditEvent API (use `create_audit_event()`)
- [ ] T051 Structure routing decision metadata (matched rules, target users, channels, suppressed, filtered)
- [ ] T052 Include routing performance metrics (routing_time_ms)
- [ ] T053 Handle audit logging errors gracefully (don't block routing)
- [ ] T054 Add type hints to all audit service methods
- [ ] T055 [P] Add metrics for audit logging (audit events created, failures)

#### Constitutional Alignment
- Principle V (Security): Audit trail for all routing decisions
- Principle VI (Reliability): Graceful error handling, don't block routing

#### Implementation Notes
- Use B09 `create_audit_event()` service function
- Event type: `notification_routing_decision`, category: `notification_routing`
- Metadata structure per data-model.md RoutingDecisionLog
- Redact sensitive payload data (log structure, not content)

#### Parallel Opportunities
- Metrics (T055) parallel with core logic

#### Dependencies
- Depends on B09 AuditEvent API
- Depends on WP03-WP06 (all routing services provide data to log)

#### Risks & Mitigations
- Audit logging failures → Log warning, don't raise exception to caller
- Large metadata objects → Limit payload logging, use summaries

---

### Work Package WP08: Celery Task Integration & Async Processing

**Goal**: Wire together all services into Celery task that processes events asynchronously.
**Independent Test**: Emit event via EventService, verify Celery task executes routing → preferences → suppression → B16 → audit flow.
**Prompt**: `tasks/planned/WP08-celery-task-integration.md`
**Priority**: P1 (Core functionality for User Story 1)

#### Included Subtasks
- [ ] T056 Complete `route_event_task` Celery task implementation in `tasks/routing_tasks.py`
- [ ] T057 Wire service calls: EventService → RoutingService → PreferenceService → SuppressionService → HandoffService → AuditService
- [ ] T058 Implement error handling and task retries (max 3 retries, exponential backoff)
- [ ] T059 Add task execution time metrics
- [ ] T060 Configure Celery task routing and queue (use default queue)
- [ ] T061 Add structured logging for task lifecycle (start, success, failure, retry)
- [ ] T062 Test idempotency (same event processed twice doesn't duplicate notifications)

#### Constitutional Alignment
- Principle VI (Performance): Async processing, non-blocking
- Principle VI (Reliability): Retry logic, error handling, logging

#### Implementation Notes
- Task signature: `route_event_task(event_dict: dict) -> None`
- Flow: validate → route → filter preferences → check suppression → dispatch to B16 → log audit
- Use Celery's `autoretry_for=(Exception,)` with exponential backoff
- Idempotency: suppression service ensures duplicates are caught

#### Parallel Opportunities
- None (sequential service calls)

#### Dependencies
- Depends on WP02-WP07 (all services must exist)
- Depends on B15 (Celery configured)

#### Risks & Mitigations
- Task failures → Retry logic with backoff, alert on persistent failures
- Queue backlog → Monitor queue depth, scale workers if needed

---

## Phase 2: Configuration & Policies (Priority: P2)

### Work Package WP09: Organisation Notification Policies & Quiet Hours

**Goal**: Implement organisation-level policies including quiet hours rate limiting.
**Independent Test**: Org with quiet hours policy delivers notifications at configured rate limit during quiet hours.
**Prompt**: `tasks/planned/WP09-org-policies-quiet-hours.md`
**Priority**: P2 (User Story 3)

#### Included Subtasks
- [ ] T063 Create `services/policy_service.py` with `get_org_policy()` method
- [ ] T064 Implement quiet hours detection (check current time in org timezone)
- [ ] T065 Implement rate limiting during quiet hours (Redis counter with TTL)
- [ ] T066 Integrate policy checks into routing flow (after preference filtering)
- [ ] T067 Add timezone handling (use pytz, org policy timezone field)
- [ ] T068 Add structured logging for policy application (quiet hours triggered, rate limit applied)
- [ ] T069 [P] Add metrics for policy enforcement (quiet hours hits, rate-limited notifications)

#### Constitutional Alignment
- Principle II (Architecture): Policy service encapsulates policy logic
- Principle VI (Performance): Rate limiting via Redis, no DB calls per event

#### Implementation Notes
- Rate limiting: Redis key `rate_limit:{org_id}:{minute_bucket}` with TTL 60 seconds
- Increment counter, check against `quiet_hours_rate_limit` (default 10)
- If rate exceeded, queue notification for delivery after quiet hours (Celery ETA)
- Timezone conversion: use `pytz.timezone(policy.quiet_hours_timezone)`

#### Parallel Opportunities
- Logging and metrics (T068-T069) parallel with core logic

#### Dependencies
- Depends on WP01 (OrganisationNotificationPolicy model)
- Depends on WP03-WP05 (routing services)

#### Risks & Mitigations
- Timezone errors → Validate timezone on policy save, use fallback to UTC
- Rate limit accuracy → Use minute-level buckets, accept approximate limits

---

## Phase 3: Admin & Developer Experience (Priority: P3)

### Work Package WP10: DRF Admin APIs & Routing Log Queries

**Goal**: Provide DRF REST APIs for admin users to query routing decision logs and configure preferences.
**Independent Test**: Admin can query routing logs via API, filter by event type/org/user, paginated results.
**Prompt**: `tasks/planned/WP10-drf-admin-apis.md`
**Priority**: P3 (User Story 5 - debugging support)

#### Included Subtasks
- [ ] T070 Create DRF serializers in `serializers/routing_serializers.py` (RoutingRule, NotificationPreference, policy)
- [ ] T071 Create DRF viewsets for routing log queries in `views/routing_logs_views.py`
- [ ] T072 Implement filtering (event_type, org_id, user_id, timestamp range)
- [ ] T073 Implement pagination (page_size=50 default)
- [ ] T074 Add permissions (org admin for org logs, superadmin for all logs)
- [ ] T075 [P] Create DRF views for user preference CRUD in `views/preference_views.py`
- [ ] T076 [P] Wire up URL routing in `urls.py`
- [ ] T077 Add API documentation (DRF browsable API)

#### Constitutional Alignment
- Principle VII (API Design): DRF standards, consistent responses, pagination, versioning
- Principle V (Security): Permission-based access, no unauthorized access to logs

#### Implementation Notes
- Query B09 AuditEvent with category="notification_routing"
- Use DRF filter backends for query params
- Permissions: `IsAuthenticated` + custom permission class for org admin
- Pagination: `PageNumberPagination` with configurable page size

#### Parallel Opportunities
- Preference views (T075) can be developed in parallel with log views (T070-T074)

#### Dependencies
- Depends on WP07 (audit logging infrastructure)
- Depends on B09 AuditEvent query API

#### Risks & Mitigations
- Large result sets → Enforce pagination, add DB indexes on filter fields
- Slow queries → Optimize B09 query performance, cache common queries

---

### Work Package WP11: Management Commands & Seed Data

**Goal**: Provide management commands to seed default routing rules and configure common event types.
**Independent Test**: Run `configure_routing` command, verify default rules created in database.
**Prompt**: `tasks/planned/WP11-management-commands-seed-data.md`
**Priority**: P3 (Developer experience)

#### Included Subtasks
- [ ] T078 Create `management/commands/configure_routing.py` command
- [ ] T079 Implement default rule creation for common events (project.*, org.*, task.*)
- [ ] T080 Add --dry-run flag for preview
- [ ] T081 Add --force flag to overwrite existing rules
- [ ] T082 [P] Create migration `0002_seed_default_routing_rules.py` for data seeding
- [ ] T083 Document command usage in README
- [ ] T084 Add command to quickstart.md guide

#### Constitutional Alignment
- Principle VIII (Developer Experience): Easy setup, scripted configuration
- Principle XI (Documentation): Command documented in app README

#### Implementation Notes
- Seed rules: global scope, in_app channel, normal priority
- Event types: project.created, project.updated, project.deleted, project.member_added, org.member_invited
- Use `get_or_create()` to avoid duplicates
- Print summary of rules created/skipped

#### Parallel Opportunities
- Data migration (T082) can be created in parallel with command development

#### Dependencies
- Depends on WP01 (RoutingRule model)

#### Risks & Mitigations
- Duplicate rules → Use unique constraints, check before creating
- Migration failures → Test migration rollback

---

### Work Package WP12: Documentation & Quickstart Integration

**Goal**: Update documentation with integration examples, troubleshooting guides, and quickstart scenarios.
**Independent Test**: Developer follows quickstart, successfully emits event and sees notification delivered.
**Prompt**: `tasks/planned/WP12-documentation-quickstart.md`
**Priority**: P3 (Developer experience)

#### Included Subtasks
- [x] T085 Create `src/contextual_notifications/README.md` with architecture overview
- [x] T086 Document event emission API with code examples
- [x] T087 Document routing rule configuration (admin + command)
- [x] T088 Create troubleshooting guide (why didn't I get notified?)
- [x] T089 [P] Update quickstart.md with working end-to-end examples
- [x] T090 [P] Add ADR for routing rule evaluation order (`docs/adr/005-routing-evaluation-order.md`)
- [x] T091 [P] Add ADR for suppression strategy (`docs/adr/006-suppression-strategy.md`)

#### Constitutional Alignment
- Principle XI (Documentation): In-repo docs, app README, getting started, extension guide, ADRs
- Principle VIII (Developer Experience): Clear examples, easy setup

#### Implementation Notes
- README sections: Overview, Event Emission, Routing Rules, Debugging, Architecture
- Troubleshooting: Common issues (no rules configured, user opted out, Redis unavailable)
- ADRs: Document key decisions from research.md (explicit columns, Redis TTL, direct B16 calls)

#### Parallel Opportunities
- All documentation tasks (T085-T091) can proceed in parallel

#### Dependencies
- Depends on WP02-WP11 (all features implemented)

#### Risks & Mitigations
- Outdated docs → Keep docs in sync with code, review during PR
- Missing examples → Test all code examples before committing

---

## Phase 4: Testing & Quality (Priority: P3)

### Work Package WP13: Unit & Integration Tests

**Goal**: Achieve 90%+ test coverage with unit tests for services and integration tests for full event→notification flow.
**Independent Test**: Run `pytest tests/contextual_notifications/` with coverage report showing 90%+ coverage.
**Prompt**: `tasks/planned/WP13-unit-integration-tests.md`
**Priority**: P3 (Quality gates)

#### Included Subtasks
- [ ] T092 Create test fixtures in `tests/contextual_notifications/conftest.py` (orgs, projects, users, rules, preferences)
- [ ] T093 [P] Write model tests in `tests/contextual_notifications/models/`
- [ ] T094 [P] Write EventService unit tests in `tests/contextual_notifications/services/test_event_service.py`
- [ ] T095 [P] Write RoutingService unit tests in `tests/contextual_notifications/services/test_routing_service.py`
- [ ] T096 [P] Write PreferenceService unit tests in `tests/contextual_notifications/services/test_preference_service.py`
- [ ] T097 [P] Write SuppressionService unit tests in `tests/contextual_notifications/services/test_suppression_service.py`
- [ ] T098 [P] Write PolicyService unit tests in `tests/contextual_notifications/services/test_policy_service.py`
- [ ] T099 Write integration test: event emission → B16 notification in `tests/contextual_notifications/integration/test_event_to_notification_flow.py`
- [ ] T100 Write integration test: preference override in `tests/contextual_notifications/integration/test_preference_override.py`
- [ ] T101 Write integration test: quiet hours rate limiting in `tests/contextual_notifications/integration/test_quiet_hours.py`
- [ ] T102 Write integration test: suppression window in `tests/contextual_notifications/integration/test_suppression_window.py`
- [ ] T103 Configure pytest coverage thresholds (90% minimum) in `pyproject.toml`

#### Constitutional Alignment
- Principle IV (Testing): pytest + pytest-django, 90%+ coverage, regression tests, integration tests
- Principle X (Quality Gates): Coverage thresholds enforced in CI

#### Implementation Notes
- Mock B16 NotificationService in tests (use `unittest.mock.patch`)
- Mock Redis in unit tests (use `fakeredis` or `unittest.mock`)
- Use Celery's `task_always_eager=True` for synchronous task execution in tests
- Fixtures: reusable test data for orgs, projects, users, routing rules, preferences
- Integration tests: full flow from event emission to B16 call verification

#### Parallel Opportunities
- All service tests (T094-T098) can be written in parallel
- Integration tests (T099-T102) can be written in parallel once unit tests pass

#### Dependencies
- Depends on WP02-WP09 (all services implemented)

#### Risks & Mitigations
- Flaky tests → Use deterministic fixtures, mock time-dependent logic
- Slow tests → Use pytest-xdist for parallel execution, optimize fixtures

---

## MVP Scope Recommendation

**Work Packages WP01-WP08** constitute the MVP:
- WP01-WP02: Foundation (app structure, event service)
- WP03-WP07: Core routing (rule evaluation, preferences, suppression, B16 integration, audit logging)
- WP08: Async processing (Celery task wiring)

This delivers User Stories 1, 2, 4, 5 (core routing, preferences, suppression, debugging).

**Post-MVP** (WP09-WP13):
- WP09: Organisation policies (User Story 3)
- WP10-WP12: Admin APIs, commands, documentation
- WP13: Comprehensive testing

---

## Task Statistics

- **Total Subtasks**: 103 (T001-T103)
- **Work Packages**: 13 (WP01-WP13)
- **MVP Subtasks**: 62 (T001-T062 in WP01-WP08)
- **Parallel Opportunities**: 42 subtasks marked [P]

## Dependency Graph Summary

```
WP01 (Models) → WP02 (Event Service) → WP03 (Routing) → WP04 (Preferences) → WP05 (Suppression) → WP06 (B16 Integration) → WP07 (Audit) → WP08 (Celery Task)
                                             ↓
                                          WP09 (Policies)
                                             ↓
                                    WP10 (Admin APIs)
                                             ↓
                                    WP11 (Commands)
                                             ↓
                                    WP12 (Documentation)
                                             ↓
                                    WP13 (Testing)
```

## Next Steps

1. Generate prompt files for each work package using this tasks.md as input
2. Review prompts for completeness and clarity
3. Begin implementation with WP01 (Django app scaffolding)
4. Run `/spec-kitty.implement WP01` to start first work package
