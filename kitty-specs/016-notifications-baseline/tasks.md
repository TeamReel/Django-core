# Work Packages: Notifications Baseline

**Inputs**: Design documents from `kitty-specs/016-notifications-baseline/`
**Prerequisites**: plan.md (required), spec.md (user stories), research.md, data-model.md, quickstart.md

**Organization**: Fine-grained subtasks (`Txxx`) roll up into work packages (`WPxx`). Each work package must be independently deliverable and testable.

**Prompt Files**: Each work package references a matching prompt file in `kitty-specs/016-notifications-baseline/tasks/planned/` generated by `/spec-kitty.tasks`. Treat this file as the high-level checklist; keep deep implementation detail inside the prompt files.

**Constitutional Compliance**: All tasks must align with Django Core-App Constitution principles. Reference `.kittify/memory/constitution.md` for governance rules.

## Subtask Format: `[Txxx] [P?] Description`
- **[P]** indicates the subtask can proceed in parallel (different files/components).
- Include precise file paths or modules.

## Path Conventions
- **Django project**: `src/notifications/`, `tests/notifications/`
- **Absolute paths** when referencing from repo root: `C:\Users\brian\Documents\django-core\.worktrees\016-notifications-baseline\`

---

## Work Package WP01: Setup & Django App Foundation (Priority: P0)

**Goal**: Create notifications Django app structure, configure tooling, and establish quality gates per Constitution Principles III, VIII, X.
**Independent Test**: App loads in Django without errors; linting, formatting, type checking pass; tests run successfully.
**Prompt**: `kitty-specs/016-notifications-baseline/tasks/done/WP01-setup-django-app-foundation.md`

### Included Subtasks
- [X] T001 Create notifications Django app in `src/notifications/`
- [X] T002 Add app to INSTALLED_APPS in Django settings
- [X] T003 [P] Create app directory structure: models/, serializers/, views/, services/, tasks/
- [X] T004 [P] Configure mypy for type checking on notifications module
- [X] T005 [P] Setup pytest-django fixtures for notifications tests in `tests/notifications/conftest.py`
- [X] T006 Create base test classes for notifications in `tests/notifications/base.py`
- [X] T007 Add notifications app to CI pipeline (ensure linting/formatting/tests run)
- [X] T008 Create `src/notifications/__init__.py` with app metadata

### Constitutional Alignment
- Principle II (Architecture): Single-responsibility Django app, clear structure
- Principle III (Code Quality): Type hints (T004), Python 3.12+
- Principle IV (Testing): pytest-django setup (T005-T006)
- Principle X (CI/CD): CI integration (T007)

### Implementation Notes
- Use Django's `startapp` or manual creation
- Directory structure matches plan.md (models/, serializers/, views/, services/, tasks/)
- Type hints required for all public APIs
- Test fixtures should cover: Notification, NotificationType, RetryPolicy, DeliveryAttempt models

### Parallel Opportunities
- App structure (T003) and mypy configuration (T004) can proceed in parallel
- Test setup (T005-T006) can proceed once app structure exists

### Dependencies
- None (starting package).

### Risks & Mitigations
- Import conflicts with existing apps → Use unique app label 'notifications'
- Circular imports → Keep models/, services/, tasks/ clearly separated

---

## Work Package WP02: Core Data Models & Migrations (Priority: P0)

**Goal**: Implement all notification models with migrations, indexes, validation per data-model.md and Constitution Principles II, VI.
**Independent Test**: Migrations run cleanly; models can be created/queried; validation rules enforced; baseline data seeded.
**Prompt**: `kitty-specs/016-notifications-baseline/tasks/planned/WP02-core-data-models-migrations.md`

### Included Subtasks
- [X] T009 [P] Create RetryPolicy model in `src/notifications/models/retry_policy.py` with type hints
- [X] T010 [P] Create NotificationType model in `src/notifications/models/notification_type.py` with type hints
- [X] T011 [P] Create Notification model in `src/notifications/models/notification.py` with type hints
- [X] T012 [P] Create DeliveryAttempt model in `src/notifications/models/delivery_attempt.py` with type hints
- [X] T013 Create model manager with query optimization helpers (select_related, prefetch_related)
- [X] T014 Add model validation methods (clean() for recipient format, payload size)
- [X] T015 Create initial migration (0001_initial.py) with all models, indexes
- [X] T016 Create data migration (0002_seed_baseline_data.py) for default RetryPolicy and NotificationType
- [X] T017 [P] Write unit tests for RetryPolicy model in `tests/notifications/models/test_retry_policy.py`
- [X] T018 [P] Write unit tests for NotificationType model in `tests/notifications/models/test_notification_type.py`
- [X] T019 [P] Write unit tests for Notification model in `tests/notifications/models/test_notification.py`
- [X] T020 [P] Write unit tests for DeliveryAttempt model in `tests/notifications/models/test_delivery_attempt.py`
- [X] T021 Test migration rollback scenarios

### Constitutional Alignment
- Principle II (Architecture): Clear model separation, FKs define relationships
- Principle III (Code Quality): Type hints on all models (T009-T012)
- Principle IV (Testing): Comprehensive model tests (T017-T020)
- Principle VI (Performance): Indexes in migration (T015), query optimization (T013)

### Implementation Notes
- Follow data-model.md entity specifications exactly
- Indexes: idx_status_created, idx_recipient_user, idx_metadata_gin, idx_type_channel
- Validation: Email RFC 5322, URL format, payload size limits (channel-specific)
- Seeded data: "best-effort" RetryPolicy (3 attempts/1 hour), "default" NotificationType
- Use Django's JSONField with GIN indexes for metadata and payload
- Model managers should prevent N+1 queries (select_related for type, prefetch_related for attempts)

### Parallel Opportunities
- All four models (T009-T012) can be developed in parallel by different engineers
- Tests (T017-T020) can be written in parallel once model contracts are defined

### Dependencies
- Depends on WP01 (app structure must exist).

### Risks & Mitigations
- Migration failures → Test migrations in clean database before commit
- Index performance on large tables → Monitor query performance in production, adjust indexes if needed
- JSONField compatibility → Ensure PostgreSQL version supports JSONField and GIN indexes

---

## Work Package WP03: Email Channel Implementation (Priority: P1) 🎯 MVP ✅ APPROVED

**Goal**: Implement email notification delivery via SMTP with async Celery tasks, retry logic, and delivery tracking (User Story 1).
**Independent Test**: Can create email notification via API → Celery task queued → SMTP delivery attempted → status updated → delivery attempts recorded.
**Prompt**: `kitty-specs/016-notifications-baseline/tasks/done/WP03-email-channel-implementation.md`
**Status**: ✅ **APPROVED** (2025-12-01) - Production ready with 16/16 unit tests passing (100%)

### Included Subtasks
- [X] T022 Create NotificationChannel ABC in `src/notifications/channels/base.py` with type hints
- [X] T023 Implement EmailChannel in `src/notifications/channels/email.py` with SMTP delivery
- [X] T024 Create Django email templates in `src/notifications/templates/notifications/email/default_*.txt|html`
- [X] T025 Implement template rendering service in `src/notifications/services/template_service.py`
- [X] T026 Create Celery task for async email delivery in `src/notifications/tasks/delivery_tasks.py`
- [X] T027 Integrate Celery retry mechanism with RetryPolicy model (map policy to task retries)
- [X] T028 Implement DeliveryAttempt creation in delivery task (before each attempt)
- [X] T029 Add email recipient validation (RFC 5322 compliance) to EmailChannel
- [X] T030 Handle SMTP errors (distinguish permanent vs transient failures)
- [X] T031 Update Notification status atomically (pending → sent/failed with row locking)
- [X] T032 [P] Write unit tests for EmailChannel in `tests/notifications/channels/test_email.py`
- [X] T033 [P] Write unit tests for TemplateService in `tests/notifications/services/test_template_service.py`
- [X] T034 [P] Write integration tests for email delivery in `tests/notifications/integration/test_email_delivery.py`
- [X] T035 Add structured logging for email delivery events (sent, failed, retrying)
- [X] T036 Configure SMTP settings in Django settings with environment variables

### Constitutional Alignment
- Principle I (Product-Agnostic): Generic email channel, no product logic
- Principle II (Architecture): ABC for channels, clear separation of concerns
- Principle III (Code Quality): Type hints (T022-T023), Django templates (T024)
- Principle IV (Testing): Unit (T032-T033) and integration tests (T034)
- Principle V (Security): SMTP credentials in env vars (T036), no email content in logs
- Principle VI (Performance): Async delivery (T026), atomic status updates (T031)

### Implementation Notes
- NotificationChannel ABC defines: send(), validate_recipient(), validate_config()
- EmailChannel uses Django's send_mail() with configured SMTP backend
- Templates: default_subject.txt, default_body.html, default_body.txt
- Celery task maps RetryPolicy fields to task.retry(countdown=X, max_retries=Y)
- SMTP response codes: 5xx permanent → no retries, 4xx transient → retry
- DeliveryAttempt created with attempt_number, attempted_at before send()
- Row locking: use select_for_update() when updating Notification.status
- Mock SMTP in tests using unittest.mock or pytest-mock

### Parallel Opportunities
- EmailChannel (T023) and template service (T025) can develop in parallel
- Tests (T032-T034) can be written once channel interface (T022) is defined

### Dependencies
- Depends on WP02 (models must exist).
- Depends on B15 (Celery infrastructure).

### Risks & Mitigations
- SMTP timeouts → Configure timeout (30s), treat as transient failure
- Email delivery failures → Retry logic handles transient failures; log permanent failures for manual intervention
- Template rendering errors → Validate templates on app startup, fail fast
- Celery task failures → B15 handles task retry; log to B09 for monitoring

---

## Work Package WP04: Retry Policies & Delivery Tracking (Priority: P2) ✅ APPROVED

**Goal**: Implement configurable retry policies with exponential backoff, respect retry windows, track all delivery attempts (User Story 2).
**Independent Test**: Can configure custom RetryPolicy → notifications respect max_attempts and retry_window → exponential backoff calculated correctly → attempt history queryable.
**Prompt**: `kitty-specs/016-notifications-baseline/tasks/done/WP04-retry-policies-delivery-tracking.md`
**Status**: ✅ **APPROVED** - All 11 subtasks complete, 14/14 tests passing (100%), reviewed 2025-12-02

### Included Subtasks
- [X] T037 Implement retry delay calculation (exponential backoff) in `src/notifications/services/retry_service.py`
- [X] T038 Add retry window enforcement (skip retry if outside window)
- [X] T039 Implement retry policy loader in Celery task (load from NotificationType → RetryPolicy)
- [X] T040 Add Celery task countdown calculation based on policy parameters
- [X] T041 Create DeliveryAttempt record before each retry attempt
- [X] T042 Update Notification status to "failed" when max_attempts exhausted
- [X] T043 Add metrics for retry attempts (prometheus counters)
- [X] T044 [P] Write unit tests for RetryService in `tests/notifications/services/test_retry_service.py`
- [X] T045 [P] Write integration tests for retry behavior in `tests/notifications/integration/test_retry_policies.py`
- [X] T046 Create example "critical" retry policy in migration (for documentation)
- [X] T047 Add Django admin interface for RetryPolicy management

### Constitutional Alignment
- Principle II (Architecture): RetryService encapsulates retry logic (single responsibility)
- Principle III (Code Quality): Type hints, clean calculation logic
- Principle IV (Testing): Unit (T044) and integration tests (T045)
- Principle VI (Performance & Reliability): Retry windows prevent unbounded retries
- Principle VI (Observability): Metrics (T043)

### Implementation Notes
- Exponential backoff formula: `delay = initial_delay * (backoff_multiplier ** attempt_number)`
- Cap delay at `retry_window / max_attempts` to fit within window
- Retry window check: `(now - notification.created_at) < retry_window_seconds`
- DeliveryAttempt outcome: success/transient_failure/permanent_failure
- Celery task uses self.retry(countdown=delay, max_retries=max_attempts)
- Django admin for RetryPolicy: list view, create/edit forms, read-only for seeded policies

### Parallel Opportunities
- RetryService (T037-T040) and metrics (T043) can develop in parallel
- Django admin (T047) independent of retry logic

### Dependencies
- Depends on WP03 (delivery task must exist).

### Risks & Mitigations
- Backoff calculation overflow → Cap delay at reasonable max (e.g., 1 hour)
- Retry window edge cases (timezone issues) → Use UTC consistently
- Policy changes during retry → Load policy per-attempt, not cached

---

## Work Package WP05: Notification History & Audit API (Priority: P2) ✅ DONE

**Goal**: Provide queryable API for notification history, delivery attempts, and B09 audit logging integration (User Story 3).
**Independent Test**: Can query notifications by status/type/date → results paginated → delivery attempts included → B09 audit events logged for critical transitions.
**Prompt**: `kitty-specs/016-notifications-baseline/tasks/done/WP05-notification-history-audit-api.md`
**Status**: ✅ APPROVED (reviewed 2025-12-02, 17/17 tests passing)
**Reviewed By**: claude-reviewer

### Included Subtasks
- [X] T048 Create NotificationSerializer in `src/notifications/serializers/notification_serializer.py`
- [X] T049 Create DeliveryAttemptSerializer in `src/notifications/serializers/delivery_attempt_serializer.py`
- [X] T050 Create NotificationViewSet in `src/notifications/views/notification_views.py` with list/retrieve
- [X] T051 Add query filters: status, type, date_range, recipient (using django-filter)
- [X] T052 Implement pagination (DRF PageNumberPagination, default 50, max 100)
- [X] T053 Optimize queries: select_related(notification_type), prefetch_related(delivery_attempts)
- [X] T054 Add B09 audit logging for: notification_created, notification_sent, notification_failed
- [X] T055 Hash recipient identifiers before logging to B09 (privacy protection)
- [X] T056 [P] Write unit tests for serializers in `tests/notifications/serializers/`
- [X] T057 [P] Write API tests for NotificationViewSet in `tests/notifications/views/test_notification_views.py`
- [X] T058 [P] Write integration tests for audit logging in `tests/notifications/integration/test_audit_logging.py`
- [X] T059 Add API documentation (drf-spectacular schema)

### Constitutional Alignment
- Principle II (Architecture): DRF viewsets, serializers at boundary
- Principle IV (Testing): Unit (T056) and API tests (T057-T058)
- Principle V (Security): Hashed recipients (T055), no sensitive data in logs
- Principle VI (Performance): Query optimization (T053), pagination (T052)
- Principle VII (API Design): DRF standards, filtering, pagination, validation

### Implementation Notes
- Serializers validate at boundary (required fields, format checks)
- Filters: NotificationFilter with status, type (FK), created_at (date range), recipient
- Query optimization: NotificationViewSet.get_queryset() uses select_related/prefetch_related
- B09 audit events: use audit.create_event() with event_type, actor, metadata
- Recipient hashing: SHA256(recipient) before logging (irreversible)
- DRF spectacular for OpenAPI schema generation

### Parallel Opportunities
- Serializers (T048-T049) and audit logging (T054-T055) can develop in parallel
- Tests (T056-T058) can be written once API contract defined

### Dependencies
- Depends on WP02 (models), WP03 (notification creation flow).
- Depends on B09 (audit logging).

### Risks & Mitigations
- N+1 queries → Use django-debug-toolbar in tests, monitor query count
- Pagination performance on large tables → Ensure indexes on filter fields
- Audit log volume → B09 handles retention; only log critical transitions

---

## Work Package WP06: In-App Notification Channel (Priority: P3) ✅ COMPLETE

**Goal**: Implement in-app notifications with read/unread status and user-specific queries (User Story 4).
**Independent Test**: Can create in-app notification for user → stored in database → user queries unread notifications → marks as read → status updated.
**Prompt**: `kitty-specs/016-notifications-baseline/tasks/done/WP06-in-app-notification-channel.md`
**Status**: ✅ Approved (27/27 tests passing)
**Reviewer**: claude-reviewer
**Completion Commit**: 86602bf

### Included Subtasks
- [X] T060 Implement InAppChannel in `src/notifications/channels/in_app.py`
- [X] T061 Add read_at field handling to Notification model (already exists, add logic)
- [X] T062 Create InAppNotificationViewSet in `src/notifications/views/in_app_views.py`
- [X] T063 Add user-specific filtering (recipient_user FK, requires B05 User model)
- [X] T064 Implement mark-as-read endpoint (PUT /notifications/{id}/mark-read/)
- [X] T065 Implement bulk mark-as-read endpoint (POST /notifications/mark-all-read/)
- [X] T066 Add unread_only, read_only filters to viewset
- [X] T067 Add permission checks (users can only query their own notifications)
- [X] T068 [P] Write unit tests for InAppChannel in `tests/notifications/channels/test_in_app.py`
- [X] T069 [P] Write API tests for in-app endpoints in `tests/notifications/views/test_in_app_views.py`
- [ ] T070 Add optional expiration TTL for in-app notifications (cleanup task) - DEFERRED

### Constitutional Alignment
- Principle I (Product-Agnostic): Generic in-app channel, products define notification content
- Principle II (Architecture): InAppChannel follows ABC contract
- Principle IV (Testing): Unit (T068) and API tests (T069)
- Principle V (Security): Permission checks (T067), users own their data
- Principle VII (API Design): RESTful endpoints, clear actions

### Implementation Notes
- InAppChannel.send() synchronous (no Celery), directly creates Notification with status="sent"
- recipient_user FK links to B05 User model
- Filters: unread (read_at is NULL), read (read_at is not NULL)
- Permissions: custom DRF permission class (IsOwnerOrAdmin)
- Mark-as-read sets read_at to timezone.now()
- Bulk mark-as-read: Notification.objects.filter(recipient_user=user, read_at__isnull=True).update(read_at=now)
- Optional TTL: cleanup task deletes notifications where (now - created_at) > TTL

### Parallel Opportunities
- InAppChannel (T060) and viewset (T062-T067) can develop in parallel
- Permission checks (T067) independent of channel implementation

### Dependencies
- Depends on WP02 (Notification model).
- Depends on B05 (User model).

### Risks & Mitigations
- User notification volume → Add indexes on (recipient_user, read_at, created_at)
- Permission bypass → Ensure permission checks in viewset, not just URL
- TTL cleanup performance → Run cleanup task during off-peak hours

---

## Work Package WP07: Webhook Notification Channel (Priority: P3) ✅ COMPLETE

**Goal**: Implement webhook delivery via HTTP POST with signature verification, redirect handling, and timeout enforcement (User Story 5).
**Independent Test**: Can configure webhook endpoint → create webhook notification → HTTP POST sent with signed payload → response recorded → failures retry per policy.
**Prompt**: `kitty-specs/016-notifications-baseline/tasks/done/WP07-webhook-notification-channel.md`
**Status**: ✅ Approved without changes (39/39 tests passing)
**Commits**: 03941c7, 0019efb, 31f64ab

### Included Subtasks
- [X] T071 Implement WebhookChannel in `src/notifications/channels/webhook.py`
- [X] T072 Add HMAC-SHA256 signature generation in `src/notifications/services/webhook_signature_service.py`
- [X] T073 Implement HTTP POST with timeout (30s default) using requests library
- [X] T074 Handle HTTP redirects (follow up to 30, default for requests library)
- [X] T075 Record HTTP status, response body snippet (1KB max) in DeliveryAttempt
- [X] T076 Distinguish HTTP 4xx (permanent) vs 5xx (transient) for retry logic
- [X] T077 Add webhook signing key configuration (env var WEBHOOK_SECRET_KEY)
- [X] T078 Create WebhookEndpoint model (deferred - optional feature)
- [X] T079 Validate webhook URL format (HTTP/HTTPS only)
- [X] T080 [P] Write unit tests for WebhookChannel in `tests/notifications/channels/test_webhook.py` (14 tests)
- [X] T081 [P] Write unit tests for signature service in `tests/notifications/services/test_webhook_signature_service.py` (19 tests)
- [X] T082 [P] Write integration tests with mock HTTP server in `tests/notifications/integration/test_webhook_delivery.py` (9 tests)
- [X] T083 Add webhook signature verification examples to documentation (Python, Node.js, PHP, Ruby)

### Constitutional Alignment
- Principle I (Product-Agnostic): Generic webhook channel for any integration
- Principle II (Architecture): WebhookChannel follows ABC, signature service separated
- Principle III (Code Quality): Type hints, clean HTTP handling
- Principle IV (Testing): Unit (T080-T081) and integration tests (T082)
- Principle V (Security): HMAC signatures (T072), secret in env var (T077)
- Principle VI (Performance & Reliability): Timeout enforcement (T073), retry logic (T076)

### Implementation Notes
- WebhookChannel.send() uses requests.post(url, json=payload, timeout=30, allow_redirects=True)
- Signature format: `X-Notification-Signature: t=<timestamp>,v1=<hmac_sha256_hex>`
- HMAC payload: `timestamp + '.' + notification_id + '.' + json.dumps(payload)`
- Redirect limit: max_redirects=3, detect infinite loops
- HTTP status handling: 2xx → success, 4xx → permanent_failure, 5xx → transient_failure
- Response body truncated to 1024 bytes for storage in DeliveryAttempt
- Mock HTTP server in tests using responses library or pytest-httpserver

### Parallel Opportunities
- WebhookChannel (T071) and signature service (T072) can develop in parallel
- Documentation (T083) independent of implementation

### Dependencies
- Depends on WP02 (models), WP03 (delivery task pattern).

### Risks & Mitigations
- Timeout too short for slow endpoints → Configurable via settings (default 30s)
- Response body memory usage → Truncate to 1KB max
- Signature replay attacks → Include timestamp in signature, verify timestamp freshness (±5 minutes)
- Malicious redirects → Limit redirects to 3, validate redirect URL scheme (HTTP/HTTPS only)

---

## Work Package WP08: Observability, Metrics & Cleanup (Priority: P2) ✅ DONE

**Goal**: Add Prometheus metrics, retention cleanup task, and operational health checks per Constitution Principle VI.
**Independent Test**: Metrics exposed at /metrics → cleanup task deletes old notifications → health checks pass.
**Prompt**: `kitty-specs/016-notifications-baseline/tasks/done/WP08-observability-metrics-cleanup.md`

### Included Subtasks
- [X] T084 Add Prometheus metrics in `src/notifications/metrics.py`
- [X] T085 Implement notification counters: created_total, sent_total, failed_total (by type, channel)
- [X] T086 Implement delivery duration histogram (notification_delivery_duration_seconds)
- [X] T087 Create retention cleanup Celery task in `src/notifications/tasks/cleanup_tasks.py`
- [X] T088 Schedule cleanup task with celery-beat (daily, off-peak hours)
- [X] T089 Add optional archival to cleanup task (export to S3/cold storage before delete)
- [X] T090 Create health check endpoint in `src/notifications/views/health_views.py`
- [X] T091 Add SMTP connectivity check to health endpoint
- [X] T092 Add Celery queue depth metric to health endpoint
- [X] T093 [P] Write unit tests for metrics in `tests/notifications/test_metrics.py`
- [X] T094 [P] Write unit tests for cleanup task in `tests/notifications/tasks/test_cleanup_tasks.py`
- [X] T095 [P] Write integration tests for health checks in `tests/notifications/integration/test_health_checks.py`
- [X] T096 Add structured logging for cleanup operations (number deleted, errors)

### Constitutional Alignment
- Principle VI (Performance & Reliability): Metrics (T084-T086), health checks (T090-T092)
- Principle VI (Observability): Structured logging (T096), Prometheus integration
- Principle IV (Testing): Unit (T093-T094) and integration tests (T095)

### Implementation Notes
- Use prometheus_client library (already in project via B06 django-prometheus)
- Metrics: Counter (created/sent/failed), Histogram (delivery duration with buckets)
- Cleanup task: Notification.objects.filter(created_at__lt=90_days_ago).delete()
- Cleanup runs daily at 2 AM UTC (configurable via celery-beat schedule)
- Health check: GET /health/ returns {status: ok/degraded/down, checks: {...}}
- SMTP check: attempt connection to SMTP server (don't send email)
- Queue depth: query Celery broker for pending task count
- Archival: optional, export to S3 as JSONL before deletion

### Parallel Opportunities
- Metrics (T084-T086) and cleanup task (T087-T089) can develop in parallel
- Health checks (T090-T092) independent of metrics/cleanup

### Dependencies
- Depends on WP02 (models), WP03 (delivery tasks).
- Depends on B15 (Celery).

### Risks & Mitigations
- Cleanup task performance on large tables → Batch deletes (e.g., 1000 at a time)
- Metrics memory usage → Use prometheus_client efficiently, avoid unbounded labels
- Health check failures alerting → Configure monitoring to alert on health check failures

---

## Work Package WP09: Documentation & Quickstart Validation (Priority: P2)

**Goal**: Create comprehensive documentation, validate quickstart guide, create ADR for retry policies per Constitution Principle XI.
**Independent Test**: Quickstart guide works end-to-end → documentation covers all channels → ADR explains retry design.
**Prompt**: `kitty-specs/016-notifications-baseline/tasks/planned/WP09-documentation-quickstart-validation.md`

### Included Subtasks
- [ ] T097 Create `docs/notifications-baseline.md` with architecture overview
- [ ] T098 Add channel configuration guide (email SMTP, webhook endpoints)
- [ ] T099 Add retry policy examples (best-effort, critical)
- [ ] T100 Add API reference (auto-generated from drf-spectacular)
- [ ] T101 Create troubleshooting guide (common errors, solutions)
- [ ] T102 Create `docs/notifications-extension-guide.md` for custom channels
- [ ] T103 Add custom notification type creation guide
- [ ] T104 Add webhook signature verification examples (Python, Node.js, PHP)
- [ ] T105 Create ADR-016-notification-retry-policies.md in `docs/adr/`
- [ ] T106 Validate quickstart.md end-to-end (create notification, check status, query history)
- [ ] T107 Update README.md with notifications section
- [ ] T108 Add inline code comments and docstrings (Google style)

### Constitutional Alignment
- Principle XI (Documentation): Comprehensive docs (T097-T104), ADR (T105)
- Principle VIII (Developer Experience): Clear examples, troubleshooting guide

### Implementation Notes
- Architecture overview: diagram of channels, retry flow, B15/B09 integration
- Configuration guide: SMTP settings, webhook signing key generation
- Retry examples: show "best-effort" (3/1 hour) vs "critical" (10/24 hours)
- API reference: use drf-spectacular to generate OpenAPI schema, render with Swagger/Redoc
- Troubleshooting: pending notifications (Celery not running), SMTP errors (credentials), webhook signatures (verification failure)
- Extension guide: subclass NotificationChannel, implement send(), register in settings
- ADR: explain why exponential backoff, why per-type policies, alternatives considered
- Quickstart validation: run through guide in clean environment, verify each step

### Parallel Opportunities
- All documentation tasks (T097-T108) can proceed in parallel once implementation complete

### Dependencies
- Depends on WP03-WP08 (implementation must be complete).

### Risks & Mitigations
- Documentation drift → Update docs in same PR as implementation changes
- Quickstart failures → Test guide in CI with fresh database

---

## Summary

**Total Work Packages**: 9 (WP01-WP09)
**Total Subtasks**: 108 (T001-T108)

**MVP Scope** (P0-P1): WP01, WP02, WP03, WP04, WP05
- Covers: Setup, models, email channel, retry policies, history API
- Delivers: User Story 1 (email with tracking), User Story 2 (retry policies), User Story 3 (history)

**Phase 2** (P2): WP08, WP09
- Covers: Observability, cleanup, documentation
- Delivers: Production-ready monitoring, retention policies, comprehensive docs

**Phase 3** (P3): WP06, WP07
- Covers: In-app and webhook channels
- Delivers: User Story 4 (in-app), User Story 5 (webhooks)

**Parallelization Highlights**:
- WP01 and WP02: Models can be developed in parallel (T009-T012)
- WP03: EmailChannel and TemplateService can develop in parallel
- WP04: RetryService and metrics can develop in parallel
- WP05: Serializers and audit logging can develop in parallel
- All test writing can proceed in parallel once contracts defined

**Constitutional Compliance**: All work packages validated against 12 constitution principles. No violations requiring justification.

**Next Suggested Command**: Review tasks.md, confirm work package breakdown, then run implementation starting with MVP packages (WP01-WP05).
