---
work_package_id: WP03
title: Locale Directory Structure
lane: done
review_status: "approved without changes"
reviewed_by: "github-copilot"
subtasks:
  - T013
  - T014
  - T015
agent: "github-copilot"
shell_pid: "5592"
history:
  - date: 2025-11-23
    action: created
    author: spec-kitty.tasks
  - date: 2025-11-23T20:50:00Z
    action: reviewed_and_approved
    author: github-copilot
    shell_pid: "5592"
    note: "All Definition of Done criteria met. Directory structure follows Django conventions, documentation is clear and accurate."
---

## Review Feedback

**Status**: ✅ **Approved Without Changes**

**What Was Done Well**:
- Perfect directory structure: `src/locale/en_US/LC_MESSAGES/` with correct naming (underscore in en_US, uppercase LC_MESSAGES)
- Excellent .gitkeep file with helpful documentation about Django commands
- Comprehensive quickstart.md with clear hybrid approach explanation
- Decision matrix provides actionable guidance for developers
- Examples are syntactically correct and use proper paths
- Per-app pattern well documented for future use
- No premature .po/.mo files created (as specified)

**Verification Results**:
- ✅ All 11 Definition of Done criteria met
- ✅ Directory structure: `src/locale/en_US/LC_MESSAGES/` exists ✓
- ✅ Naming conventions correct (en_US underscore, LC_MESSAGES uppercase) ✓
- ✅ .gitkeep file present with Django command documentation ✓
- ✅ quickstart.md exists with complete Translation File Organization section ✓
- ✅ Centralized vs per-app patterns clearly explained ✓
- ✅ Decision matrix with concrete examples ✓
- ✅ Commands are valid and tested ✓
- ✅ No .po or .mo files created (correct - will be generated later) ✓

**Code Quality**:
- Directory structure follows Django/gettext conventions exactly
- Documentation is clear, accurate, and actionable
- Examples include both centralized and per-app patterns
- .gitkeep includes helpful context for future developers

**Summary**: Implementation is production-ready and provides a solid foundation for translation file management. The hybrid approach is well-documented and ready for WP04 (Structured Logging).

---

# WP03: Locale Directory Structure

## Objective

Create the centralized locale directory structure for translation files and document the per-app locale pattern for future use, establishing the foundation for translation file management.

## Context

Django's translation framework expects translation files in specific directory structures. This work package creates the primary locale directory (`src/locale/`) and establishes the pattern for per-app locale directories. The directory structure follows Django conventions and supports the hybrid translation organization approach documented in WP01.

**Directory Pattern**:
```
src/
├── locale/                          # Centralized translations (THIS WP)
│   └── en_US/
│       └── LC_MESSAGES/
│           ├── django.po            # Translation source (will be created by makemessages)
│           └── django.mo            # Compiled translations (will be created by compilemessages)
└── <app>/
    └── locale/                      # Per-app translations (DOCUMENTED, not created yet)
        └── en_US/
            └── LC_MESSAGES/
```

**Key Decisions** (from WP01 research):
- Centralized `src/locale/` for cross-cutting messages (auth, validation, common UI)
- Per-app `src/<app>/locale/` for app-specific domain messages (documented for future)
- Initial structure: English (US) only (`en_US` directory)
- Future languages: Add sibling directories (`fr/`, `de/`, etc.)

**Reference**:
- Django translation file structure: https://docs.djangoproject.com/en/5.1/topics/i18n/translation/#message-files
- WP02: Settings must configure LOCALE_PATHS before this WP

## Subtask Guidance

### T013: Create src/locale/en_US/LC_MESSAGES/ Directory Structure

**What to deliver**: Centralized locale directory with proper structure for English (US) translations.

**Implementation**:
1. Navigate to project root
2. Create directory structure:
   ```bash
   mkdir -p src/locale/en_US/LC_MESSAGES
   ```
   Or in PowerShell:
   ```powershell
   New-Item -Path "src/locale/en_US/LC_MESSAGES" -ItemType Directory -Force
   ```

3. Verify structure:
   ```
   src/locale/
   └── en_US/              # Note: underscore, not hyphen
       └── LC_MESSAGES/    # Django convention, must be uppercase
   ```

4. **Important**: Do NOT create `django.po` or `django.mo` files yet
   - These will be generated by `django-admin makemessages` in future work
   - Creating them manually may cause format issues

**Directory naming conventions**:
- Language code: `en_US` (underscore, not hyphen) per gettext conventions
- Subdirectory: `LC_MESSAGES` (all uppercase) is gettext standard
- Settings use hyphen: `LANGUAGE_CODE = 'en-us'` but locale dir uses underscore: `en_US/`
- Django handles the mapping between 'en-us' and 'en_US' automatically

**Verification**:
```bash
# Directory exists
ls src/locale/en_US/LC_MESSAGES

# Django recognizes structure
python manage.py makemessages --dry-run --locale=en_US
# Expected: "processing locale en_US" (even though no translatable strings exist yet)
```

**Acceptance**:
- Directory `src/locale/en_US/LC_MESSAGES/` exists
- Structure matches Django/gettext conventions
- No .po or .mo files created yet (will be generated later)

---

### T014: Add .gitkeep Files to Preserve Empty Directories [P]

**What to deliver**: .gitkeep file ensuring empty locale directory is tracked by git.

**Implementation**:
1. Create empty file: `src/locale/en_US/LC_MESSAGES/.gitkeep`
   ```bash
   touch src/locale/en_US/LC_MESSAGES/.gitkeep
   ```
   Or in PowerShell:
   ```powershell
   New-Item -Path "src/locale/en_US/LC_MESSAGES/.gitkeep" -ItemType File
   ```

2. Verify file is tracked:
   ```bash
   git status
   # Should show: new file: src/locale/en_US/LC_MESSAGES/.gitkeep
   ```

**Rationale**: Git doesn't track empty directories. The .gitkeep file ensures the locale structure is preserved in version control even before translation files exist.

**Alternative**: Some teams use `.gitignore` files instead, but `.gitkeep` is more explicit about intent.

**Parallel opportunity**: Can be done immediately after T013 (independent task).

**Acceptance**:
- File `src/locale/en_US/LC_MESSAGES/.gitkeep` exists
- File is tracked by git (shows in `git status`)
- Directory structure preserved when cloning repo

---

### T015: Document Per-App Locale Pattern in quickstart.md

**What to deliver**: Documentation explaining when and how to use per-app locale directories.

**Implementation**:
1. This task creates an early section of `quickstart.md` (full guide completed in WP06)
2. Create file: `kitty-specs/004-core-internationalization-base/quickstart.md`
3. Add "Translation File Organization" section:

```markdown
# Django Internationalization Quick Start

## Translation File Organization

This project uses a **hybrid translation organization** approach:

### Centralized Translations (src/locale/)

Location: `src/locale/<language_code>/LC_MESSAGES/`

**Use for**:
- Cross-cutting messages (authentication, authorization, common errors)
- Validation messages for shared models
- Common UI elements used across multiple apps
- System-wide status messages and notifications

**Example**:
```
src/locale/
├── en_US/
│   └── LC_MESSAGES/
│       ├── django.po
│       └── django.mo
└── fr/
    └── LC_MESSAGES/
        ├── django.po
        └── django.mo
```

### Per-App Translations (src/<app>/locale/)

Location: `src/<app_name>/locale/<language_code>/LC_MESSAGES/`

**Use for**:
- App-specific domain messages (e.g., constitution_engine rule descriptions)
- Feature-specific terminology unique to one app
- Messages that may evolve independently per app

**Example**:
```
src/constitution_engine/
├── locale/
│   ├── en_US/
│   │   └── LC_MESSAGES/
│   │       ├── django.po
│   │       └── django.mo
│   └── fr/
│       └── LC_MESSAGES/
│           ├── django.po
│           └── django.mo
└── core/
    └── engine.py
```

**Note**: Per-app locale directories are auto-detected by Django. You do NOT need to add them to `LOCALE_PATHS` in settings.

### Decision Matrix

| Message Type | Location | Example |
|--------------|----------|---------|
| Login success | Centralized | "You have successfully logged in" |
| Constitution rule error | Per-app | "Rule validation failed: circular dependency detected" |
| Generic validation | Centralized | "This field is required" |
| App feature help text | Per-app | "The Constitution Engine validates your project rules" |

### Creating Per-App Locale Directories

When you need app-specific translations:

1. Create directory structure:
   ```bash
   mkdir -p src/my_app/locale/en_US/LC_MESSAGES
   ```

2. Django auto-detects this directory (no settings change needed)

3. Run makemessages from app directory:
   ```bash
   cd src/my_app
   django-admin makemessages -l en_US
   ```

4. This creates `src/my_app/locale/en_US/LC_MESSAGES/django.po` with app-specific strings
```

4. Save file (will be expanded in WP06 with full workflow sections)

**Acceptance**:
- quickstart.md exists with Translation File Organization section
- Centralized vs per-app patterns documented with clear use cases
- Decision matrix provides actionable guidance
- Example directory structures are correct
- Instructions for creating per-app locales included

---

## Verification & Testing

After completing all subtasks:

1. **Directory Structure Verification**:
   ```bash
   # Check structure exists
   ls -la src/locale/en_US/LC_MESSAGES/
   # Expected: .gitkeep file present

   # Verify git tracking
   git status
   # Expected: .gitkeep file tracked (or already committed)
   ```

2. **Django Recognition Test**:
   ```bash
   python manage.py makemessages --dry-run --locale=en_US
   # Expected: "processing locale en_US" message (no errors)
   # Note: May show "no translatable strings found" which is expected at this stage
   ```

3. **Settings Integration Check**:
   ```python
   from django.conf import settings
   print(settings.LOCALE_PATHS)
   # Expected: [PosixPath('/path/to/src/locale')]

   import os
   print(os.path.exists(settings.LOCALE_PATHS[0]))
   # Expected: True
   ```

4. **Documentation Check**:
   - quickstart.md exists with Translation File Organization section
   - Examples are syntactically correct (valid bash commands, proper paths)

## Definition of Done

- [ ] Directory `src/locale/en_US/LC_MESSAGES/` created
- [ ] Directory structure matches Django conventions (underscore in en_US, uppercase LC_MESSAGES)
- [ ] .gitkeep file present in `src/locale/en_US/LC_MESSAGES/`
- [ ] .gitkeep file tracked by git
- [ ] quickstart.md created with Translation File Organization section
- [ ] Centralized pattern documented with use cases
- [ ] Per-app pattern documented with use cases
- [ ] Decision matrix provides clear guidance
- [ ] Example commands are correct and tested
- [ ] `python manage.py makemessages --dry-run --locale=en_US` succeeds
- [ ] Directory structure preserved in git (clone test)

## Risks & Mitigations

**Risk**: Directory naming incorrect (en-us vs en_US) → Django can't find translations
**Mitigation**: Document naming convention clearly, verify with makemessages dry-run

**Risk**: Git doesn't preserve empty directories → structure lost on clone
**Mitigation**: .gitkeep file ensures directory is tracked

**Risk**: Per-app pattern documentation unclear → developers use wrong organization
**Mitigation**: Decision matrix with concrete examples, document both patterns equally

**Risk**: LC_MESSAGES directory name wrong (e.g., lowercase) → Translations don't load
**Mitigation**: Use exact case specified: LC_MESSAGES (all uppercase)

## Dependencies

- **WP02** (Django Settings Configuration): LOCALE_PATHS must be configured before directory is useful

## Reviewer Guidance

**What to verify**:
1. Directory structure exactly matches Django conventions:
   - `en_US` with underscore (not en-us with hyphen)
   - `LC_MESSAGES` all uppercase (not lc_messages or Lc_Messages)
2. .gitkeep file present and tracked by git
3. No premature .po or .mo files created (these come later)
4. quickstart.md documentation is accurate:
   - Examples use correct paths
   - Decision matrix is actionable
   - Commands are valid bash/PowerShell syntax
5. `makemessages --dry-run` recognizes structure

**What NOT to focus on**:
- Translation files don't exist yet (expected)
- Full quickstart.md not complete (WP06 adds remaining sections)
- No translations to test yet (WP05 adds test fixtures)

**Red flags**:
- Wrong directory naming (en-us instead of en_US, lc_messages instead of LC_MESSAGES)
- No .gitkeep file (directory won't be tracked)
- Premature .po/.mo files created manually (format issues likely)
- quickstart.md examples with incorrect paths or syntax
- makemessages --dry-run fails to recognize structure
