"""
URL routing for {{ app_name }} API.
"""

from django.urls import include, path
from rest_framework.routers import DefaultRouter

from .views import {{ model_name | default("Item") }}ViewSet

app_name = "{{ app_name }}"

# DRF router for viewsets
router = DefaultRouter()
router.register(r"{{ model_name | default('items') | lower }}", {{ model_name | default("Item") }}ViewSet, basename="{{ model_name | default('item') | lower }}")

urlpatterns = [
    path("", include(router.urls)),
]
