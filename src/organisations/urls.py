"""
URL configuration for organisations app.

API URLs will be configured in organisations/api/urls.py (WP03).
"""

app_name = "organisations"

urlpatterns = [
    # API URLs will be included in WP03
]
